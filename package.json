{
  "scripts": {
    "postinstall": "cd frontend && npm install --production=false && npm run build",
    "start": "node server.js",
    "dev": "env $(cat .env | xargs) npm start"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": ">=5.0.0-alpha.8",
    "faster-word-id": "^0.1.0",
    "mongoose": "^5.12.7",
    "ws": "^7.4.5"
  },
  "version": "0.1.0",
  "engines": {
    "node": "16.x"
  }
}
